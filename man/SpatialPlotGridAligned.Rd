% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Seurat_Spatial_based.R
\name{SpatialPlotGridAligned}
\alias{SpatialPlotGridAligned}
\title{SpatialPlotGridAligned}
\usage{
SpatialPlotGridAligned(
  seurat_obj,
  group.by = "seurat_clusters",
  spacing = 2,
  ncol = 2,
  label.by = "Tissue",
  col_vector = NULL
)
}
\arguments{
\item{seurat_obj}{A \code{\link[Seurat]{Seurat}} object containing spatial metadata columns
\code{x_slide_mm}, \code{y_slide_mm}, \code{SampleID}, and the grouping and labeling columns.}

\item{group.by}{Character. Name of the metadata column to use for coloring points (e.g.,
\code{"seurat_clusters"}).}

\item{spacing}{Numeric. Vertical and horizontal spacing (in same units as slide coordinates)
between panels in the grid.}

\item{ncol}{Integer. Number of columns in the output grid.}

\item{label.by}{Character. Name of the metadata column to use for panel titles
(e.g., \code{"Tissue"} or \code{"SampleID"}).}

\item{col_vector}{Named character vector. Custom colors for each level of \code{group.by}.
Names must match the factor levels; if \code{NULL}, a \code{viridis} discrete palette is used.}
}
\value{
A \code{\link[ggplot2]{ggplot}} object showing all samples arranged in a grid,
  colored by the specified grouping and labeled by the specified metadata.
}
\description{
Arrange multiple samples from a Seurat object in a grid layout using their spatial coordinates,
preserving within‐sample morphology and annotating each panel with a metadata label.
}
\examples{
\dontrun{
  # Default palette, 2 columns
  SpatialPlotGridAligned(SerObjIntg, group.by = "seurat_clusters",
                         spacing = 20, ncol = 2, label.by = "Tissue")

  # Custom color vector
  my_colors <- c("0" = "#440154", "1" = "#414487", "2" = "#2A788E")
  SpatialPlotGridAligned(SerObjIntg, col_vector = my_colors)
}

}
