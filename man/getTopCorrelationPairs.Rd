% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/EnrichmentAnalysis.R
\name{getTopCorrelationPairs}
\alias{getTopCorrelationPairs}
\title{Get Top Correlation Pairs from a Correlation Matrix}
\usage{
getTopCorrelationPairs(corMat, nTop = 5)
}
\arguments{
\item{corMat}{A square numeric matrix representing correlations between variables. Should have row and column names.}

\item{nTop}{An integer specifying the number of top positive and negative correlation pairs to return. Defaults to 5.}
}
\value{
A list with two data frames:
\describe{
  \item{topPositive}{Data frame of the top \code{nTop} most positively correlated variable pairs.}
  \item{topNegative}{Data frame of the top \code{nTop} most negatively correlated variable pairs.}
}
}
\description{
Extracts the top positively and negatively correlated variable pairs from a correlation matrix.
}
\examples{
# Generate a random correlation matrix
set.seed(123)
mat <- matrix(rnorm(100), ncol = 10)
corMat <- cor(mat)
result <- getTopCorrelationPairs(corMat, nTop = 3)
result$topPositive
result$topNegative

}
